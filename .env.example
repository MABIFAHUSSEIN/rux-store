# Postgres config
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rux_ecommerce
DB_USER=rux_user
DB_PASSWORD=your_db_password

# JWT config
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=1h

# Google client ID for token verification (OIDC)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
